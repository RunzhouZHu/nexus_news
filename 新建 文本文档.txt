# Terminal 1 — ingestion (polls NewsAPI every 5 min)
cd nexus
npm run ingest

# Terminal 2 — pipeline worker (processes the queue)
npm run worker


docker compose up -d        # starts postgres + redis
npm run ingest &
npm run worker

